# Always select a separate project: -p secrecon-test
services:
  api:
    profiles: [api]
  object-store:
    environment:
      AWS_ACCESS_KEY_ID: ${S3_ACCESS_KEY}
      AWS_SECRET_ACCESS_KEY: ${S3_SECRET_KEY}

