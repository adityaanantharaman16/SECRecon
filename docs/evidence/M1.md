# M1: source preservation and financial provenance

2026-09-09: the isolated Linux test suite passes **21 tests**, including real PostgreSQL/Redis/S3 integration. Ruff formatting/lint and strict mypy pass.

Verified: five duplicate processing runs converge; exact decimal values survive; fact JSON locators and filing-document provenance remain accessible; conflicting observations are retained; malformed sources and corrupted archived copies quarantine without partial facts; conditional S3 creation rejects different bytes; a manifest without SQL registration can be reprocessed; a blob without a manifest is not a committed event. Mock HTTP responses verify archived 429 bodies/Retry-After, truncated payload labeling, durable transport failure records and shared 403 pause.

Run: `docker compose -p secrecon-test -f compose.yaml -f compose.test.yaml run --build --rm test`.

The initial real SEC fixture gate was pending contact approval. The owner explicitly authorized the configured Git email on 2026-09-16; the earlier rejected command made no requests. The gate is now complete with the recorded sources described below. Synthetic fixtures remain clearly labeled.

The local S3 credential can administer its test store; immutability is enforced through the application's conditional-create interface and verified collision detection. This is not administrator-proof WORM. M3 adds the automated archive-to-SQL inventory sweeper; M1 verifies its underlying recovery boundary explicitly.

## 2026-09-16: real-source completion

Ten successful bounded requests captured submissions, Company Facts and primary documents for Rivian, Amazon and Robinhood. Each decoded HTTP body is preserved losslessly as gzip with its URL, UTC retrieval time, checksum and byte length. The identifying contact stays in ignored local configuration. The recorder now refuses to replace an existing capture.

Robinhood's original `0001783879-26-000023` and amendment `0001783879-26-000029` report on December 31, 2025. The amendment's explanatory note identifies formatting corrections without changed financial results. Manual inspection verified **38,137,000,000 USD** in year-end Assets in both documents and their accession-specific Company Facts observations. The contract tests check the inline XBRL unit, instant context and scale independently, and follow each normalized locator back into the captured JSON. See [fixture notes and source links](../../tests/fixtures/recorded/README.md).

The real-service test imports the four Robinhood sources into S3/PostgreSQL and processes them five times. Canonical output remains identical, exactly four processing runs exist, both accession-specific assertions survive, and each has its own primary-document provenance. Additional contract checks cover all three captured companies and checksum/identity validation.

Full gate: `docker compose -p secrecon-test -f compose.yaml -f compose.test.yaml run --build --rm test` — **54 passed in 39.10 seconds**, lint/format/strict types pass, domain/job coverage **89.47%**. Two existing upstream deprecation warnings remain. The default host gate separately passes 15 tests with integration tests intentionally skipped. Local evidence log: `.local/m1-real-fixture-gate.log` (ignored).

M1 is complete locally. This verifies source preservation, normalization and provenance; general amendment reconciliation remains M4. Hosted CI has not run because no remote is configured.
