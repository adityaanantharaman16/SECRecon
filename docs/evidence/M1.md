# M1: source preservation and financial provenance

2026-09-09: the isolated Linux test suite passes **21 tests**, including real PostgreSQL/Redis/S3 integration. Ruff formatting/lint and strict mypy pass.

Verified: five duplicate processing runs converge; exact decimal values survive; fact JSON locators and filing-document provenance remain accessible; conflicting observations are retained; malformed sources and corrupted archived copies quarantine without partial facts; conditional S3 creation rejects different bytes; a manifest without SQL registration can be reprocessed; a blob without a manifest is not a committed event. Mock HTTP responses verify archived 429 bodies/Retry-After, truncated payload labeling, durable transport failure records and shared 403 pause.

Run: `docker compose -p secrecon-test -f compose.yaml -f compose.test.yaml run --build --rm test`.

The real SEC fixture gate is pending approval of a contact address for the SEC User-Agent. Automated approval review rejected using the configured Git email without destination-specific authorization. No real fixture request was made by that rejected command. Synthetic fixtures are clearly labeled; they are not claimed as SEC evidence.

The local S3 credential can administer its test store; immutability is enforced through the application's conditional-create interface and verified collision detection. This is not administrator-proof WORM. M3 adds the automated archive-to-SQL inventory sweeper; M1 verifies its underlying recovery boundary explicitly.
