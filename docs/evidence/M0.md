# M0: reproducible foundation

Validated 2026-09-09 on Windows with Docker Desktop's Linux engine and Python 3.12.14.

- `python scripts/check.py`: Ruff, format and strict mypy pass; offline unit tests pass.
- `docker compose -p secrecon-test -f compose.yaml -f compose.test.yaml run --build --rm test`: **6 passed**, no skipped integration tests; real PostgreSQL 17, Redis 7.4 and SeaweedFS.
- Empty isolated database migrated to revision `0001`; API liveness/readiness return 200 and readiness checks the expected migration head.
- Restarted the isolated database, Redis and object store; the database persistence probe remained present and dependency tests passed again.
- Pinned service/base images by digest and Python dependencies in `uv.lock`. A non-root container cache-permissions defect was found and fixed during the isolated rehearsal.
- GitHub CI workflow is prepared and locally rehearsed. No remote is configured, so hosted CI execution remains pending; no remote success is claimed.

Try: `python scripts/bootstrap.py`, then `docker compose -f compose.yaml -f compose.dev.yaml up -d --build`, then open http://localhost:8000/docs. For host commands on this workstation, use `.venv/Scripts/python.exe`; uv was bootstrapped into ignored `.tools/bin`.

The Linux test container emits upstream Starlette/httpx deprecation warnings. They do not fail checks. Dependency changes must continue through the locked compatibility suite.
