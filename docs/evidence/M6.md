# M6: failure and performance evidence

## M6.1 implementation status

The fixture-driven isolated failure harness is implemented but the real-service acceptance gate is not yet complete in this environment.

Implemented:

- `scripts/service_drills.py` accepts only base names matching `secrecon-drill-<short-unique-name>`, generates a fresh child project name for every run, proves that no labeled containers/volumes/networks already use that child name before claiming cleanup ownership, always supplies `-p` plus `compose.yaml` and `compose.test.yaml`, and rejects the development, release and ordinary test project names before invoking Docker.
- The database-outage/object-store-restart scenario is defined by `tests/fixtures/failure_drills/database_outage.json`.
- Each run creates a fresh drill key, records Git/Docker/Compose/Python versions and Docker CPU/memory/OS/architecture, starts only the isolated test stack, captures before/after authoritative SQL job, attempt, event and outbox invariants, checks Redis acknowledgement state and immutable source bytes, validates every attempt span against the persisted job trace context, waits boundedly for object storage after startup/restart, bounds all subprocess waits, records successful/failed/timed-out command outputs/return codes/timings plus total duration, writes a JSON report under ignored `.local/failure-drills/`, and removes only the child project it established as unused.
- `--runs 3` provides the required consecutive-run gate without weakening cleanup or isolation between runs.

No job-ownership, comparison, projection, archive or telemetry semantics changed. The harness reuses the existing SQL store, transactional outbox, queue, worker, fenced lease recovery, archive and OpenTelemetry context propagation described by ADRs 0002–0004.

## Verification completed on 2026-09-22

Safety refusal (runs before fixture loading or any Compose subprocess):

```text
/opt/data/projects/SECRecon/.venv/bin/python scripts/service_drills.py --project secrecon
SAFETY REFUSAL: refusing unsafe Compose project 'secrecon'; use secrecon-drill-<short-unique-name>
(exit 2)
```

Host check:

```text
/opt/data/projects/SECRecon/.venv/bin/python scripts/check.py
Ruff: passed
Ruff format: passed (102 files)
mypy strict: passed (43 source files)
pytest: 51 passed, 60 skipped, 2 warnings in 5.17s (111 collected)
```

The skipped tests are the existing real-service integration suite because this execution environment has no Docker daemon. `docker info` returned: `Cannot connect to the Docker daemon at unix:///var/run/docker.sock`.

## Required gate still pending

Run these from an environment with a working Docker daemon and committed local `.env` settings:

```text
python scripts/service_drills.py --project secrecon-drill-m6-gate --runs 3
docker compose -p secrecon-test -f compose.yaml -f compose.test.yaml run --build --rm test
```

Before closing M6.1, record the three JSON report paths plus each run's SQL invariants, trace ID and duration; record the full integration pass count, branch-inclusive coverage and duration; and review `git diff`. This document does not claim those Docker-backed results yet.

M6 performance baselines remain a separate later slice. M7 still owns tested full backup restore and release rollback; this failure drill does not replace those gates.
